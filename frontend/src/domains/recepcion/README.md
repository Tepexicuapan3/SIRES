# Frontend Domain Scaffold: recepcion

Base del dominio Recepcion para extraccion incremental desde `frontend/src/features/recepcion`.

Estado inicial: `legacy`.

## Estructura

- `components/`
- `hooks/`
- `pages/`
- `state/`
- `adapters/`
- `types/`

## Guardrail

Coexistencia `features` + `domains` hasta cerrar DoD del dominio.
