# Frontend Domain Scaffold: auth-access

Base del dominio Auth & Access en frontend domain-first.

Estado inicial: `legacy` (runtime principal sigue en `frontend/src/features/auth` y `frontend/src/features/admin`).

## Estructura

- `components/`
- `hooks/`
- `pages/`
- `state/`
- `adapters/`
- `types/`

## Guardrail

No mover rutas activas en Fase 1. Solo estructura y contratos base.
