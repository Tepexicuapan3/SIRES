import { useRoutes } from "react-router-dom";
import { farmaciaRoutes } from "@routes/modules/farmacia.routes.config";

const FarmaciaRoutes = () => useRoutes(farmaciaRoutes);

export default FarmaciaRoutes;
