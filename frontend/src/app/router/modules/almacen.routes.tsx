import { useRoutes } from "react-router-dom";
import { almacenRoutes } from "@routes/modules/almacen.routes.config";

const AlmacenRoutes = () => useRoutes(almacenRoutes);

export default AlmacenRoutes;
