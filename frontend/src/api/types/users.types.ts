/**
 * Types para Users API
 *
 * Este archivo re-exporta todos los tipos inferidos de Zod para mantener
 * compatibilidad con código existente que importa desde aquí.
 *
 * Los tipos reales están definidos en: @api/schemas/users.schema
 */

export type * from "@api/schemas/users.schema";
