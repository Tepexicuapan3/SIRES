/**
 * Types for Roles API
 *
 * Re-export de tipos inferidos desde Zod schemas
 * Los tipos se generan automáticamente desde @api/schemas/roles.schema
 */

export type * from "@api/schemas/roles.schema";
