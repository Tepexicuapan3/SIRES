import { useRoutes } from "react-router-dom";
import { recepcionRoutes } from "@/routes/modules/recepcion.routes.config";

const RecepcionRoutes = () => useRoutes(recepcionRoutes);

export default RecepcionRoutes;
