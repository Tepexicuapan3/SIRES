from django.apps import AppConfig


class PortalCitasConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.portal_citas"
    verbose_name = "Portal de Citas en Línea"
