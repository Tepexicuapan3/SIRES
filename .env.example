# ============================================
# VARIABLES GLOBALES DEL PROYECTO SIRES
# ============================================

# Puertos de los servicios
BACKEND_PORT=5000
FRONTEND_PORT=5173

# ============================================
# NOTA IMPORTANTE:
# ============================================
# Este archivo contiene solo las variables compartidas a nivel de Docker Compose.
# 
# Para variables específicas:
# - Backend (Flask): copiar backend/.env.example → backend/.env
# - Frontend (Vite): copiar frontend/.env.example → frontend/.env
#
# Las variables del backend incluyen la conexión a MySQL local.
# Las variables del frontend deben comenzar con VITE_ para ser accesibles.
# ============================================
