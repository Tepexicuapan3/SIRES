#!/usr/bin/env bash

set -eu

ROOT_DIR="$(git rev-parse --show-toplevel)"
EXPORT_SCRIPT="$ROOT_DIR/.engram/scripts/export-on-commit-msg.sh"

if [ ! -x "$EXPORT_SCRIPT" ]; then
  echo "Error: falta permiso de ejecucion en $EXPORT_SCRIPT" >&2
  exit 1
fi

"$EXPORT_SCRIPT"
